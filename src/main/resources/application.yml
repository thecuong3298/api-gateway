server:
  port: 8080
spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: openapi
          uri: http://localhost:${server.port}
          predicates:
            - Path=/v3/api-docs/**
          filters:
            - RewritePath=/v3/api-docs/(?<path>.*), /$\{path}/v3/api-docs
        - id: cms-master-service
          uri: http://localhost:8090
          predicates:
            - Path=/cms-master/**
        - id: authorization-service
          uri: http://localhost:8081
          predicates:
            - Path=/oauth/**
